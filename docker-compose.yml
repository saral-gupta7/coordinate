services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: coordinate-api
    restart: unless-stopped
    env_file:
      - ./backend/.env
    ports:
      - "127.0.0.1:8000:8000"
